import 'package:flutter/material.dart';

class StatelessSamplewidget extends StatelessWidget {
  const StatelessSamplewidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(


    );
  }
}
